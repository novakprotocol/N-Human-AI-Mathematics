# Private public-switch preflight correction R4 - 2026-07-27

## Scope

This correction finishes the private release gate without changing the
HINC-001 revised manuscript or any mathematical claim.

## Corrections completed in this gate

1. The private preflight pins ordinary `github.com` for every GitHub CLI call.
2. Its dependency environment is outside the repository source tree.
3. It compiles both the materialized verifier module and compatibility entry point.
4. It uses the supported `--maximum-order` verifier option.
5. It excludes release-gate detection vocabularies from public prose scans.
6. It hashes the final immutable log rather than a pre-finalization version.
7. The private site exposes the model/provider non-disclosure boundary in the status row.
8. The status-row CSS update is scoped to the complete `.status-row` rule; the process-list grid is unchanged.
9. The Pages workflow compiles the actual materialized verifier module.
10. The final switch record supersedes stale local kit identities.

## Earlier corrections already present at the governed starting head

- the compatibility verifier imports its materialized sibling module;
- the dual-state repository workflow validates private and eventual public states;
- citation terminology matches the binary-Gerstenhaber manuscript;
- the source manifest records materialization in the standalone repository.

## Claim effect

No theorem, formula, proof, formal receipt, manuscript byte, publication order,
authorship statement, or historical-priority boundary changes.

## Release effect

Public visibility remains prohibited until this corrected private preflight
passes against the exact repaired PR head.