# HINC-001 complete-manuscript Lean completion map

## Status

```text
paper:                         HINC-001
current formal state:          PARTIAL_PASS
full manuscript Lean verified: no
active theorem release:        HOLD
```

This map defines the remaining formal obligations. It does not claim completion.

## Literal source objects

The formalization must begin with:

- a characteristic-two commutative base ring or field;
- the dual-number algebra `D = F[epsilon]/(epsilon^2)`;
- the augmentation quotient `D/F1` and its tensor powers;
- normalized Hochschild cochains and the actual differential;
- cup product, insertion, and binary Gerstenhaber bracket;
- the explicit graded subalgebras `E` and `O`;
- their functorial endomorphisms over arbitrary commutative base algebras.

Coefficient equations alone do not establish the manuscript.

## Formal lanes

### H01 — crossing and skew-extension algebraic kernel

```text
status: COMPILED_PASS
```

Covers the crossing monoid, square-zero skew extension, commutativity defect, sufficient center equations, and unit-group commutator kernel already recorded in the public bounded Lean evidence.

### H02 — principal coefficient classifications

```text
status: COMPILED_PASS
```

Covers the even and odd generator coefficient equations and normalized coefficient-level composition laws.

### H03 — initial dual-number Hochschild foundation

```text
status: COMPILED_PASS_ON_OPEN_FORMAL_BRANCH
```

Covers the square-zero generator, characteristic-two cancellation of the represented boundary value, represented cup multiplication, and the epsilon-coefficient product rule.

This is not the complete normalized cochain complex or bracket theorem.

### H04 — complete normalized cochains, cup product, and bracket

Required declarations must establish:

1. the literal augmentation quotient and tensor powers;
2. evaluation on `epsilon^(tensor n)` as a linear equivalence between normalized cochains and `D`;
3. the complete normalized Hochschild differential formula;
4. vanishing of every interior and boundary term for every degree;
5. no nonzero coboundaries;
6. the cup-product formula `c_(i,x) cup c_(j,y) = c_(i+j,xy)`;
7. the insertion formula;
8. the full binary Gerstenhaber bracket
   `[(x u^i),(y u^j)] = (i*x*partial(y)+j*y*partial(x))u^(i+j-1)`;
9. all degree-zero and boundary cases.

```text
status: OPEN
```

### H05 — even source algebra and generator-to-global theorem

Required declarations must establish:

- `E = F + uD[u]`;
- the presentation `E ~= F[r,s]/(s^2)` with `r=u`, `s=epsilon*u`;
- normal forms in every degree;
- extension of the verified generator assignment to a unique unital degree-preserving algebra map;
- global cup-product and binary-bracket preservation iff the displayed coefficient conditions hold;
- naturality in arbitrary commutative characteristic-two base algebras.

```text
status: OPEN
```

### H06 — odd source presentation and global theorem

Required declarations must establish:

- the literal subalgebra `O = D + F epsilon u + u^2D[u]`;
- the stated presentation on `epsilon,a,b,c`;
- the complete odd normal-form theorem;
- reduction of every homogeneous candidate to the diagonal form;
- extension to and uniqueness of the global endomorphism;
- global cup-product and binary-bracket preservation;
- all small-degree and parity boundary cases.

```text
status: OPEN
```

### H07 — representability and coordinate bialgebras

Required declarations must establish natural equivalences between endomorphism functors and the stated affine schemes, including:

- the even coordinate ring and constrained upper-triangular comultiplication;
- the odd coordinate ring and coordinatewise composition;
- unit and associativity identities for the bialgebra structures;
- functoriality and inverse constructions over every base algebra.

```text
status: OPEN
```

### H08 — complete scheme-theoretic centers and geometry

Required declarations must establish:

- the complete even scheme-center equality, not only sufficient equations;
- the distinction between scheme center and pointwise center;
- the stated odd commutativity result;
- reduction, component, connectedness, normalization, and tangent-space claims;
- idempotent-scheme classifications;
- retained finite Artin and finite-field counting consequences and zeta formulas.

```text
status: OPEN
```

### H09 — clopen split, units, and fppf-derived subgroup

Required declarations must establish:

- the group-like idempotent and functorial clopen decomposition;
- the torus unit component and crossing ideal;
- the even unit-group semidirect-product description;
- the commutator morphism;
- the fppf sheaf generated by its image;
- `[G_E,G_E]_fppf = alpha_2` and `G_E^ab = G_m` under the manuscript convention.

```text
status: OPEN
```

## Claim-map acceptance

The final manuscript-to-Lean matrix must include every retained HINC claim ID and every displayed mathematical formula. Acceptance requires:

```text
total retained mathematical claims = mapped compiled claims
unmapped mathematical claims       = 0
unresolved formal claims           = 0
unmapped displayed formulas        = 0
unmapped boundary cases            = 0
```

## Proof integrity gates

The accepted exact-head build requires:

- zero `sorry`;
- zero `admit`;
- zero project axioms standing in for HINC results;
- no `sorryAx`;
- `#print axioms` for every exported theorem;
- an explicit standard-axiom allowlist;
- disclosure of any native or trusted-code expansion;
- a committed dependency manifest and unchanged manifest hash;
- complete build and checker logs;
- independent manuscript-to-Lean fidelity review.

## Current decision

```text
H01: PASS
H02: PASS
H03: bounded PASS on open branch
H04-H09: OPEN
FULL_PASS: NO
ACTIVE THEOREM RELEASE: HOLD
```
