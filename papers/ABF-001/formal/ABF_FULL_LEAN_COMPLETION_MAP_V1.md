# ABF-001 complete-manuscript Lean completion map

## Status

```text
paper:                         ABF-001
current formal state:          PARTIAL_PASS
compiled lane:                 A01
open lanes:                    A02-A06
full manuscript Lean verified: no
active theorem release:        HOLD
```

This map defines the remaining formal obligations. It does not claim completion.

## Literal source objects

The formalization must begin with:

- the field `GF(2)`;
- finite Boolean cubes and truth tables;
- algebraic-normal-form coefficients and algebraic degree;
- the exact vectorial map used by the manuscript;
- affine-hyperplane parameters and literal restrictions;
- vector moments, output masks, moment spans, and affine-parameter maps.

A precomputed rank table or imported JSON count is not a proof of the finite atlas.

## Formal lanes

### A01 — bidual moment-kernel bridge

```text
status: COMPILED_PASS
```

The compiled lane establishes that annihilation of every displayed moment generator is equivalent to annihilation of their span and to membership in the compatible affine-parameter kernel.

### A02 — Reed–Muller duality and moment criterion

Required declarations must establish:

1. the Boolean-function space on `d` variables;
2. monomial functions and algebraic-normal-form expansion;
3. algebraic degree and all extended boundary conventions;
4. the Reed–Muller subspace generated by monomials of degree at most `r`;
5. the standard truth-table inner product over `GF(2)`;
6. the exact duality `RM(r,d)^perp = RM(d-r-1,d)` for every retained boundary case;
7. equivalence between the degree bound and vanishing of all moments of orders at most `r`.

```text
status: OPEN
```

### A03 — affine coordinates and complete moment spaces

Required declarations must establish:

- the literal affine parameter `q=(beta,a)`;
- the indicator `h_q(x)=beta+a dot x`;
- the valid-hyperplane, empty-set, and whole-domain boundary cases;
- the literal vector moments `mu_J(q)`;
- linearity in `q`;
- the complete span `U_r(q)`;
- the scalar moment map `Phi_(r,v)`;
- the full Theorem 2 incidence equivalence from literal restrictions through A01;
- the fiber-count and double-counting corollaries.

```text
status: OPEN
```

### A04 — order-zero top-layer signature and punctured kernel

Required declarations must establish:

- the vectorial ANF coefficients `c_I`;
- the full coefficient `c` and missing-variable coefficients `c_j`;
- the map `M(a)`;
- the exact top-layer signature
  `R(a,b)=M(a)+(b+1)c`;
- equivalence with the XOR of `F` over the hyperplane;
- the degree-drop iff signature-zero theorem;
- the translated linear map `N(a,beta)`;
- the punctured-kernel characterization and rank-based count.

```text
status: OPEN
```

### A05 — order-one symmetric matrix pencil

Required declarations must establish:

- the augmented coordinate vector `z(x)`;
- the literal matrix
  `B_v = sum_x (v dot F(x)) z(x) z(x)^T` over `GF(2)`;
- symmetry of every `B_v`;
- the identity `Phi_(1,v)(q)=B_v q`;
- the kernel characterization of degree at most `n-3`;
- rank, radical, and affine-parameter consequences used by the finite instance.

```text
status: OPEN
```

### A06 — proof-connected complete 16-to-8 atlas

The formal development must connect the exact source identity through the complete finite universe:

```text
exact 65,536-byte truth table
→ all nonzero output masks
→ all valid affine hyperplanes
→ literal scalar restrictions
→ exact algebraic degrees
→ order-zero and order-one moments
→ matrix ranks and kernels
→ exact counts
→ radical subspaces
→ mask-intersection graph
→ mask-parameter incidence graph
→ component and forest classifications
```

Retained exact values include:

- `130,559` vectorial restrictions of degree 15;
- `511` vectorial restrictions of degree 14;
- no lower vectorial restrictions;
- rank multiplicities `13^2, 14^15, 15^74, 16^112, 17^52`;
- `203` singular mask-indexed radicals;
- `202` distinct nonzero radical subspaces;
- `469` nonzero incidences on `467` affine parameters;
- the unique triple-covered parameter;
- `K3 + 200 K1` for the mask-intersection graph;
- a `201`-component incidence forest with the stated component structure.

The accepted route must use kernel-checked exhaustive evaluation or explicit certificates checked in Lean. Importing the final numbers as assumptions is prohibited.

```text
status: OPEN
```

## Claim-map acceptance

The final manuscript-to-Lean matrix must include every retained ABF claim ID and every exact finite value. Acceptance requires:

```text
total retained mathematical claims = mapped compiled claims
unmapped mathematical claims       = 0
unresolved formal claims           = 0
unmapped displayed formulas        = 0
unmapped finite counts             = 0
unmapped boundary cases            = 0
```

## Proof integrity gates

The accepted exact-head build requires:

- zero `sorry`;
- zero `admit`;
- zero project axioms standing in for ABF results;
- no `sorryAx`;
- `#print axioms` for every exported theorem;
- a reviewed standard-axiom allowlist;
- disclosure of `native_decide`, `bv_decide`, compiler trust, external code, and metaprogramming;
- a committed unchanged dependency manifest;
- full source and truth-table identities;
- complete build, certificate, and checker logs;
- independent manuscript-to-Lean fidelity review.

## Current decision

```text
A01: PASS
A02-A06: OPEN
FULL_PASS: NO
ACTIVE THEOREM RELEASE: HOLD
```
